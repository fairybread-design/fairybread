{
  "name": "@electric/tokens",
  "version": "1.0.0",
  "description": "",
  "type": "commonjs",
  "scripts": {
    "build": "node config.js",
    "clean": "rm -rf build",
    "watch": "npm run build && chokidar \"src/**/*.js\" \"config.js\" -c \"npm run build\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "devDependencies": {
    "chokidar-cli": "^1.2.0",
    "glob": "^8.0.3",
    "style-dictionary": "3.7.1"
  }
}
